# 安全策略（Security Policy）

## 报告安全问题

**请不要用公开 Issue 报安全漏洞**（那样等于在修复前先把细节摊开）。

请用 GitHub 的私密通道：仓库页面 → **Security** → **Report a vulnerability**
（Advisories 私密报告）。

报告里尽量带上：

- 受影响的版本 / 提交号（`smartdns -v` 或 `git rev-parse HEAD`）；
- 平台与配置（Windows / Linux，相关的那几行配置；**请把 `api-token`、`-spki-pin` 之类的密钥值涂掉**）；
- 复现步骤、实际结果、你期望的结果；
- 如果能给出一个最小复现（一段配置 + 一条命令），会快很多。

## 范围

这个项目是一个本地 DNS 服务器，比较关心的方向：

- 监听侧：管理后台（`/api`）的鉴权绕过、越权；
- 上游侧：DoT / DoH / DoQ / DoH3 的证书校验问题；
- 解析侧：能把错误内容写进缓存并长期服务给客户端的缺陷（缓存投毒）；
- 构建与发布：能把恶意内容带进发布产物的缺陷。

## 处理方式

- 收到报告后会先确认能复现，再评估影响面；
- 修复会随下一个版本发布，发布说明里致谢（如果你愿意署名）；
- 本项目是个人维护的免费软件，**没有漏洞赏金**；
- 已知且暂不打算处理的问题：见仓库里的审计报告与处置状态文档（里面写清了每条的结论与理由）。
