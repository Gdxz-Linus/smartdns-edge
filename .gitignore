# Generated by Cargo
# will have compiled files and executables
/target/

/logs/

/smartdns.pid
# 运行期产物：PID 文件（含"顺手修"之后新增的 .lock 锁文件）与它默认所在的目录
/smartdns.pid*
/managed/

# Remove Cargo.lock from gitignore if creating an executable, leave it for libraries
# More information here https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html
# Cargo.lock

# These are backup files generated by rustfmt
**/*.rs.bk
**/*.rs.bak
*.bak

# 🌟「顺手修」：原来这里是 `**/*.txt` + `*.txt` —— 太宽了：仓库里正经的 .txt（比如
# tests/test_data/ 下的测试数据、RESOLVE_EXAMPLES.txt）只是"已经被跟踪"才侥幸留在 git 里，
# 任何**新增**的 .txt 都会被悄悄挡在门外。现在只挡真正不想要的两种：
# ① 你自己放在仓库根目录的速记文档；② 历史遗留的"- 副本"旧文件。
/新建 文本文档*.txt
**/*- 副本.*

# web ui
node_modules
dist

.idea/

/etc/smartdns/zones
/etc/smartdns/managed
/etc/smartdns/*.yaml
/etc/smartdns/*.json

.vscode/settings.json

/tmp

localhost.crt
localhost.key
